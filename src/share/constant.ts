import { CanvasParams } from './types'

const width = 441
const height = 441

export const CANVAS_PARAMS: CanvasParams = {
    id: 'canvas',
    size: [width, height],
    cellSize: 40,
    gridColor: '#000000',
    // todo: choose bg color
    bgColor: '#dadada',
}
